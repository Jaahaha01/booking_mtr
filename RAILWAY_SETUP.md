# คำแนะนำการตั้งค่า Railway สำหรับ Vercel

## 1. สร้างบัญชี Railway
- เข้าเว็บไซต์ https://railway.app
- สมัครสมาชิกฟรี

## 2. สร้างโปรเจคและ Database
- คลิก "New Project"
- เลือก "Database" → "Add MySQL"
- ตั้งชื่อ database (เช่น: booking-db)
- รอให้สร้างเสร็จ (ประมาณ 2-3 นาที)

## 3. เชื่อมต่อและนำเข้าข้อมูล
- ใน Railway dashboard คลิกที่ database ที่สร้าง
- ไปที่ tab "Connect"
- คัดลอก connection details หรือใช้ Railway CLI

**วิธีที่ง่าย: ใช้ Railway CLI**
```bash
# ติดตั้ง Railway CLI
npm install -g @railway/cli

# เข้าสู่ระบบ
railway login

# เชื่อมต่อกับโปรเจค
railway link

# เชื่อมต่อกับ database
railway connect mysql

# นำเข้าข้อมูล (ใน terminal ใหม่)
mysql -h 127.0.0.1 -P 3306 -u root < database.sql
mysql -h 127.0.0.1 -P 3306 -u root < add_room_schedules.sql
```

## 4. ดู Environment Variables
ใน Railway dashboard:
- ไปที่ tab "Variables"
- คัดลอกค่าเหล่านี้:
  - `MYSQLHOST`
  - `MYSQLUSER`
  - `MYSQLPASSWORD`
  - `MYSQLDATABASE`
  - `MYSQLPORT`

## 5. ตั้งค่า Environment Variables ใน Vercel
ใน Vercel Dashboard → Settings → Environment Variables:

```
DB_HOST=YOUR_MYSQLHOST
DB_USER=YOUR_MYSQLUSER
DB_PASSWORD=YOUR_MYSQLPASSWORD
DB_NAME=YOUR_MYSQLDATABASE
```

## 6. ทดสอบการเชื่อมต่อ
```bash
# ทดสอบใน local
npm run dev

# ดู logs ใน Vercel หลัง deploy
```

## ✅ ข้อดีของ Railway:
- **ฟรี**: 1GB storage, 512MB RAM
- **ง่าย**: UI ใช้งานง่าย ไม่ต้อง CLI ก็ได้
- **รวดเร็ว**: ตั้งค่าได้ใน 5 นาที
- **เชื่อถือได้**: Uptime สูง

## 📝 หมายเหตุ:
- Railway มีฟรี tier ที่เพียงพอสำหรับโปรเจคเล็ก
- ถ้าต้องการ scale สามารถ upgrade ได้
- Database จะ sleep หลังไม่มีการใช้งาน แต่จะตื่นอัตโนมัติ

## 🚀 Deploy:
หลังจากตั้งค่าเสร็จ Vercel จะ deploy อัตโนมัติและเชื่อมต่อกับ Railway ได้ทันที!</content>
<parameter name="filePath">d:\Learning\PJ1-CP\booking_meetingroom - Main\RAILWAY_SETUP.md