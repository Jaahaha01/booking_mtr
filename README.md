# ระบบจองห้องประชุม (Meeting Room Booking System)

ระบบจองห้องประชุมที่พัฒนาด้วย Next.js, TypeScript, MySQL และ Tailwind CSS

## คุณสมบัติหลัก

- 🔐 ระบบสมาชิกและการเข้าสู่ระบบ
- 📅 จองห้องประชุมแบบออนไลน์
- 📊 ดูตารางการจองแบบปฏิทิน
- 📈 สถิติการใช้งานห้องประชุม
- 📱 Responsive Design
- 🔔 ระบบแจ้งเตือน
- 👥 จัดการผู้ใช้และสิทธิ์

## เทคโนโลยีที่ใช้

- **Frontend**: Next.js 15, React 19, TypeScript
- **Styling**: Tailwind CSS
- **Database**: MySQL
- **Calendar**: FullCalendar
- **Authentication**: bcryptjs
- **Icons**: React Icons

## การติดตั้ง

### 1. Clone โปรเจค

```bash
git clone <repository-url>
cd booking_meetingroom
```

### 2. ติดตั้ง Dependencies

```bash
npm install
```

### 3. ตั้งค่าฐานข้อมูล

1. สร้างฐานข้อมูล MySQL ชื่อ `booking_db`
2. Import ไฟล์ `database.sql` เพื่อสร้างตารางและข้อมูลตัวอย่าง

```bash
mysql -u root -p < database.sql
```

### 4. ตั้งค่า Environment Variables

สร้างไฟล์ `.env.local` ในโฟลเดอร์หลัก:

```env
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=booking_db
```

### 5. รันโปรเจค

```bash
npm run dev
```

เปิดเบราว์เซอร์ไปที่ [http://localhost:3000](http://localhost:3000)

## ข้อมูลเข้าสู่ระบบ

### ผู้ดูแลระบบ (Admin)
- **Username**: admin
- **Password**: admin123

### ผู้ใช้ทั่วไป (User)
- **Username**: user1
- **Password**: user123
- **Username**: user2
- **Password**: user123

## โครงสร้างโปรเจค

```
booking_meetingroom/
├── app/
│   ├── api/                    # API Routes
│   │   ├── bookings/          # การจัดการการจอง
│   │   ├── login/             # เข้าสู่ระบบ
│   │   ├── register/          # สมัครสมาชิก
│   │   ├── profile/           # โปรไฟล์ผู้ใช้
│   │   └── rooms/             # การจัดการห้องประชุม
│   ├── components/            # React Components
│   ├── booking/               # หน้าจองห้องประชุม
│   ├── login/                 # หน้าเข้าสู่ระบบ
│   ├── register/              # หน้าสมัครสมาชิก
│   ├── profile/               # หน้าโปรไฟล์
│   └── rooms/                 # หน้าจัดการห้องประชุม
├── lib/                       # Utilities
│   ├── db.ts                  # การเชื่อมต่อฐานข้อมูล
│   └── auth.ts                # ฟังก์ชัน Authentication
├── database.sql               # SQL สำหรับสร้างฐานข้อมูล
└── README.md                  # เอกสารนี้
```

## API Endpoints

### Authentication
- `POST /api/login` - เข้าสู่ระบบ
- `POST /api/register` - สมัครสมาชิก

### Bookings
- `GET /api/bookings` - ดึงข้อมูลการจองทั้งหมด
- `POST /api/bookings` - สร้างการจองใหม่
- `GET /api/bookings/[id]` - ดึงข้อมูลการจองเฉพาะ
- `PUT /api/bookings/[id]` - อัปเดตการจอง
- `DELETE /api/bookings/[id]` - ยกเลิกการจอง

### Rooms
- `GET /api/rooms` - ดึงข้อมูลห้องประชุม
- `POST /api/rooms` - สร้างห้องใหม่
- `GET /api/rooms/availability` - ตรวจสอบห้องว่าง
- `GET /api/rooms/statistics` - สถิติการใช้งาน

### Profile
- `GET /api/profile` - ดึงข้อมูลโปรไฟล์
- `PUT /api/profile` - อัปเดตโปรไฟล์

## ฐานข้อมูล

### ตารางหลัก

1. **users** - ข้อมูลผู้ใช้
2. **rooms** - ข้อมูลห้องประชุม
3. **bookings** - ข้อมูลการจอง
4. **notifications** - การแจ้งเตือน

### ความสัมพันธ์

- `bookings.room_id` → `rooms.id`
- `bookings.user_id` → `users.id`
- `notifications.user_id` → `users.id`

## การใช้งาน

### สำหรับผู้ใช้ทั่วไป

1. **สมัครสมาชิก** - ไปที่หน้า Register เพื่อสร้างบัญชีใหม่
2. **เข้าสู่ระบบ** - ใช้ Username และ Password ที่สมัครไว้
3. **จองห้องประชุม** - เลือกห้อง วันที่ และเวลา
4. **ดูตารางการจอง** - ตรวจสอบการจองในปฏิทิน
5. **จัดการโปรไฟล์** - แก้ไขข้อมูลส่วนตัว

### สำหรับผู้ดูแลระบบ

1. **จัดการผู้ใช้** - อนุมัติ/ปฏิเสธการสมัครสมาชิก
2. **จัดการห้องประชุม** - เพิ่ม/แก้ไข/ลบห้องประชุม
3. **ดูสถิติ** - ตรวจสอบการใช้งานห้องประชุม
4. **จัดการการจอง** - ยกเลิกหรือแก้ไขการจอง

## การพัฒนา

### รันในโหมด Development

```bash
npm run dev
```

### Build สำหรับ Production

```bash
npm run build
npm start
```

### ตรวจสอบ Code Quality

```bash
npm run lint
```

## การแก้ไขปัญหา

### ปัญหาการเชื่อมต่อฐานข้อมูล

1. ตรวจสอบการตั้งค่า MySQL
2. ตรวจสอบไฟล์ `.env.local`
3. ตรวจสอบว่าฐานข้อมูล `booking_db` มีอยู่

### ปัญหาการ Login

1. ตรวจสอบว่าผู้ใช้ได้รับการอนุมัติแล้ว
2. ตรวจสอบ Username และ Password
3. ตรวจสอบการเข้ารหัส bcrypt

### ปัญหาการจอง

1. ตรวจสอบว่าห้องว่างในช่วงเวลาที่เลือก
2. ตรวจสอบการเข้าสู่ระบบ
3. ตรวจสอบข้อมูลที่กรอก

## การปรับแต่ง

### เปลี่ยนธีมสี

แก้ไขไฟล์ `tailwind.config.js` เพื่อเปลี่ยนสีหลักของระบบ

### เพิ่มฟีเจอร์ใหม่

1. สร้าง API Route ใหม่ใน `app/api/`
2. สร้าง Component ใหม่ใน `app/components/`
3. สร้างหน้าใหม่ใน `app/`

### ปรับแต่งฐานข้อมูล

แก้ไขไฟล์ `database.sql` และรันใหม่

## License

MIT License

## ผู้พัฒนา

พัฒนาด้วย ❤️ โดย [ชื่อผู้พัฒนา]

---

หากมีคำถามหรือต้องการความช่วยเหลือ กรุณาติดต่อ [อีเมล]
